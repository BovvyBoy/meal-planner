class Planner{

    constructor(planner){
        console.log(planner)
        // const {id, name, duration} = planner
        // this.id = id
        // this.name = name
        // this.duration = duration
    }

    // get liAndLinkHTML(){
    //     return `<li><a href="#" data-id="${this.id}">${this.name}</a></li>`
    // }
}